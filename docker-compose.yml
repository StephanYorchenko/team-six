version: "3"

services:
  db:
    image: postgres:13
    ports:
      - "5432:5432"
  web:
    build: .
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
